# 📔 감정일기

**날짜**: @today  
**오늘 한 줄**: 

---

## 🎭 오늘의 감정 (1~3개)
- [ ] 😄 행복
- [ ] 😊 만족
- [ ] 😍 설렘
- [ ] 😌 편안

- [ ] 😐 평온
- [ ] 😶 담담

- [ ] 😢 슬픔
- [ ] 😞 우울
- [ ] 😔 외로움

- [ ] 😰 불안
- [ ] 😥 걱정
- [ ] 😖 긴장

- [ ] 😠 화남
- [ ] 😤 답답
- [ ] 😒 짜증

- [ ] 😪 피곤
- [ ] 🥱 지침
- [ ] 😵‍💫 멍함

- [ ] 🤍 위로 필요
- [ ] 🌱 회복 중
- [ ] ☁️ 쉬고 싶음

---

## ✍️ 감정의 이유
- 있었던 일:
- 든 생각:
- 몸 상태:

---

## 🌈 감정 변화
시작 → 중간 → 끝  
(예: 😰 → 😐 → 😌)

---

## 🫶 오늘의 나에게

if(prop("대표 감정") == "😊", 2,
if(prop("대표 감정") == "😐", 1,
if(prop("대표 감정") == "😢", -1,
if(prop("대표 감정") == "😰", -1,
if(prop("대표 감정") == "😡", -2,
if(prop("대표 감정") == "😴", -1,
0))))))

if(prop("감정 점수") >= 2, "🌈 좋은 하루",
if(prop("감정 점수") == 1, "🙂 무난",
if(prop("감정 점수") == 0, "😐 보통",
if(prop("감정 점수") <= -2, "⚠️ 힘든 날",
"☁️ 조금 힘듦"))))

if(
  dateBetween(prop("날짜"), dateSubtract(now(), 7, "days"), now()),
  prop("감정 점수"),
  0
)

if(prop("주간 감정 합") >= 5, "🌈 아주 좋은 주",
if(prop("주간 감정 합") >= 2, "🙂 괜찮은 주",
if(prop("주간 감정 합") >= 0, "😐 무난한 주",
if(prop("주간 감정 합") >= -3, "☁️ 조금 힘든 주",
"⚠️ 많이 힘든 주"))))

if(
  formatDate(prop("날짜"), "YYYY-MM") == formatDate(now(), "YYYY-MM"),
  prop("감정 점수"),
  0
)

if(prop("월간 감정 합") >= 15, "🌸 전반적으로 안정",
if(prop("월간 감정 합") >= 5, "🙂 비교적 괜찮음",
if(prop("월간 감정 합") >= 0, "😐 보통",
if(prop("월간 감정 합") >= -10, "☁️ 많이 지침",
"🚨 회복 필요"))))

slice(
  [
    "오늘을 버틴 것만으로도 충분해.",
    "지금 느끼는 감정은 틀리지 않아.",
    "천천히 가도 괜찮아.",
    "완벽하지 않아도 나는 나야.",
    "오늘의 나는 최선을 다했어.",
    "쉬어가는 날도 필요해.",
    "감정은 지나가는 손님이야.",
    "지금 이 순간도 의미 있어.",
    "나를 돌보는 건 이기적인 게 아니야.",
    "괜찮아, 다시 시작하면 돼."
  ],
  floor(rand() * 10),
  floor(rand() * 10) + 1
)

slice(
  [
    "오늘 힘들었다면, 그건 네가 열심히 살았다는 증거야.",
    "울고 싶을 땐 울어도 괜찮아.",
    "지금의 너도 충분히 소중해.",
    "모든 날이 좋아야 할 필요는 없어.",
    "회복은 느려 보여도 분명히 진행 중이야.",
    "아무것도 하지 않은 날도 의미 있어.",
    "버티는 것도 하나의 용기야."
  ],
  floor(rand() * 7),
  floor(rand() * 7) + 1
)

slice(
  [
    "괜찮아.",
    "천천히.",
    "지금도 충분해.",
    "숨부터 쉬자.",
    "오늘은 여기까지.",
    "나는 안전해."
  ],
  floor(rand() * 6),
  floor(rand() * 6) + 1
)
